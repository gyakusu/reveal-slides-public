# Claude Code スタートダッシュガイド

> 対象: LLMでコード相談の経験があり、Pythonが書ける新入社員
> 前提: Claude Codeについては知識ゼロからスタート

---

## 0. このガイドの思想

Claude Codeは強力だが、「道具としての性質」を理解しないと効果が出ない。

核となる考え方は3つ。

1. **セッションを使い捨てろ** — LLMのコンテキストは有限の資源。相談と実装を同じセッションで行わず、結論をファイルに書き出して新しいセッションで次に進む。
2. **ツールに任せろ** — 集計や統計処理はPythonスクリプトで。品質管理はmypyやruffで。LLMは確率的に動く。決定的な処理はツールに委ね、LLMはそれを「使う側」に徹する。
3. **コンテキストを守れ** — 探索や分析はサブエージェントに委譲し、メインには結果だけを返させる。必要な知識は必要なときだけ読み込む。

3番目の「必要なときだけ読み込む」を、このガイドでは**段階的開示**と呼ぶ。LLMにとっての段階的開示とは、目的に応じた動的コンテキスト読み込みのことだ。CLAUDE.mdに何でも書くのではなく、特定タスクで必要になる知識はその瞬間にだけロードする。

この段階的開示を実現する最も有用な仕組みが **Skills** である。Skillsは「必要なときに、必要な知識だけを、自動的に読み込む」機構であり、コンテキストの効率的な運用に直結する。

---

## 1. インストールと最初の起動

```bash
# インストール（Node.js 18以上が必要）
npm install -g @anthropic-ai/claude-code

# プロジェクトのルートに移動して起動
cd your-project
claude
```

初回起動時にログインを求められる。それだけで準備完了。

確認コマンド:

```bash
claude --version    # バージョン確認
claude --help       # ヘルプ表示
```

---

## 2. CLAUDE.md — プロジェクトの「取扱説明書」

CLAUDE.mdはプロジェクトルートに置くMarkdownファイルで、Claude Codeがセッション開始時に毎回読む。
人間にとっての「プロジェクトのREADME」に相当するが、読者がLLMである点が異なる。

### 書くべきこと

- ビルド・テスト・リントの実行コマンド
- プロジェクト構造の概要（モノレポならアプリとパッケージの対応）
- コーディング規約のうち、ツールで強制できないもの（設計方針など）
- 「やってはいけないこと」の明示

### 書いてはいけないこと

- ツールで強制できるルール（フォーマットはprettier、型チェックはmypy）
- コードスニペットのコピペ（すぐ陳腐化する。ファイルパスで参照せよ）
- 何でもかんでも（指示が多すぎると全体的に無視されはじめる）

### CLAUDE.mdの良い例

```markdown
# Project Overview
Python 3.12 / FastAPI / PostgreSQL のバックエンドAPI。

# Commands
- テスト: pytest tests/ -x --tb=short
- 型チェック: mypy src/ --strict
- リント: ruff check src/
- 単一テスト: pytest tests/test_xxx.py::test_name -v

# Architecture
- src/api/ — ルーティング
- src/domain/ — ビジネスロジック（外部依存なし）
- src/infra/ — DB・外部API

# Rules
- domain層はinfra層をimportしてはならない
- 新しいエンドポイントには必ず対応するテストを書く
- エラーハンドリングはsrc/api/errors.pyのパターンに従う
```

### 重要な原則

- **短く保て。** 各行について「これを消したらClaudeが間違えるか？」と自問する。NOなら消す。
- **ポインタを使え。** コードをコピペするのではなく `src/api/errors.py を参照` と書く。
- **@構文でファイルを参照できる。** `@README.md` や `@docs/architecture.md` で外部ファイルを読み込める。
- **定期的に手入れしろ。** Claudeが指示を無視し始めたら、CLAUDE.mdが肥大化しているサイン。

### 配置場所の使い分け

| 場所                         | 用途                                            |
| ---------------------------- | ----------------------------------------------- |
| `~/.claude/CLAUDE.md`        | 全プロジェクト共通の個人設定                    |
| `./CLAUDE.md`                | プロジェクト固有（gitにcommitしてチームで共有） |
| `./子ディレクトリ/CLAUDE.md` | サブモジュール固有（必要時のみ読み込まれる）    |

---

## 3. 基本ワークフロー — セッションの使い捨て

最も大事な習慣: **1タスク1セッション**。

LLMのコンテキストは積み重なるほど品質が劣化する。相談フェーズとコーディングフェーズを同じセッションで行わない。

### 推奨フロー

```
[セッション1: 相談]
  「このAPIの設計方針を相談したい」
  → 議論の結論をファイルに書き出させる
  → セッション終了

[セッション2: 実装]
  「docs/plan.md に書かれた方針に従ってAPIを実装して」
  → 新鮮なコンテキストで実装に集中
  → セッション終了
```

ストレージへの逃がし方:

```
# セッション内でClaudeに指示する例
「今の議論の結論をdocs/design-decision-auth.mdに書き出して」
「実装計画をdocs/plan-user-api.mdにまとめて」
```

次のセッションではそのファイルを読むだけで済む。コンテキストウィンドウには結論だけが入り、議論の過程は入らない。

### コンテキスト管理の小技

- `/compact` — コンテキストを要約して圧縮する。長くなってきたら手動で。
- `/clear` — タスクを切り替えるときにリセット。
- `/model haiku` — 機械的な作業（リネーム、フォーマット確認など）はHaikuで十分。コスト・速度ともに優位。
- `/effort low` — 深い推論が不要な作業に。

---

## 4. LLMにやらせるな — スクリプト化と冪等性

### 原則: 決定的な処理はスクリプトに書け

LLMは確率的に動く。掛け算ですら間違える可能性がある。
決定的に正しい結果が必要な処理は、すべてスクリプトに逃がす。

悪い例:
```
「このCSVの売上列を合計して」
→ LLMが自力で計算する → 間違える可能性あり
```

良い例:
```
「このCSVの売上列を合計するPythonスクリプトを書いて、
 scripts/sum_sales.py に保存して、実行して」
→ スクリプトが確定的に計算する → 再実行も可能
```

### 品質管理もツールに委ねる

```markdown
# CLAUDE.mdに書いておくと効果的
- コード変更後は必ず `ruff check src/` と `mypy src/ --strict` を実行する
- テストは `pytest tests/ -x` で確認する
- これらが通らない状態でタスク完了とみなさない
```

さらに確実にしたいなら、CLAUDE.mdの「お願い」ではなくhooksで強制する:

```json
// .claude/settings.json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "ruff check src/ && mypy src/ --strict"
          }
        ]
      }
    ]
  }
}
```

こうすると、Claudeがファイルを編集するたびに自動でリンターと型チェッカーが走る。LLMの判断に頼らず、確定的にゲートをかける。

### mypyのもう一つの価値 — 型はLLMにとってのドキュメントである

mypyは「LLMの出力を決定論的に検証する」だけのツールではない。
型アノテーションの付いたコードは、LLMにとって**コードとドキュメントが一体化したモノリシックな資料**になる。

関数シグネチャに型が付いていれば、「関数名・引数名・引数型・戻り値型」が一箇所にまとまる。LLMは外部ドキュメントを参照せずとも、関数の契約を正確に把握できる。

**NG: 型なし — LLMは使い方を推測するしかない**

```python
def calculate_discount(price, user, campaign):
    if campaign and user.is_premium:
        return price * campaign.rate
    return price
```

LLMはこのコードを見て「`price`はintかfloatか？」「`campaign`はNoneが来うるのか、それともオブジェクトか？」「戻り値は何型か？」を文脈から推測する必要がある。推測は確率的であり、間違える。

**OK: 型あり — 関数シグネチャ自体がドキュメント**

```python
from decimal import Decimal

def calculate_discount(
    price: Decimal,
    user: PremiumUser | StandardUser,
    campaign: DiscountCampaign | None,
) -> Decimal:
    if campaign is not None and isinstance(user, PremiumUser):
        return price * campaign.rate
    return price
```

この関数を呼ぶコードをLLMに書かせるとき、型情報から以下が確定的にわかる:

- `price`は`Decimal`で渡す（`float`ではない）
- `campaign`は`None`を許容する（`Optional`である）
- 戻り値は`Decimal`で受ける

つまり、型アノテーションは「mypyを通すため」だけでなく、**LLMが正しくコードを読み書きするための最小限かつ正確なコンテキスト**として機能する。docstringが陳腐化しても、型は実行時（mypy）に検証されるため常に正確である。

---

## 5. サブエージェントへの委譲 — コンテキストを守る

ファイル探索やコード分析はコンテキストを大量に消費する。
メインのセッションでそれをやると、肝心の作業時にコンテキストが汚染されている。

### 組み込みサブエージェント

Claude Codeには最初から使えるサブエージェントがある。

- **Explore** — 読み取り専用のコードベース探索。ファイル構造やコード検索を、メインコンテキストを汚さず実行する。
- **Plan** — 計画モード時にコードベースを調査し、計画を立てる。
- **汎用サブエージェント** — 探索と変更の両方が必要な複雑なタスク用。

Claudeは自動的にこれらに委譲するが、明示的に指示もできる:

```
「Exploreサブエージェントを使って、認証関連のファイルを全部洗い出して」
「この変更の影響範囲をサブエージェントに調べさせて、結果だけ教えて」
```

### カスタムサブエージェントの作り方

`.claude/agents/` にMarkdownファイルを置く:

```markdown
<!-- .claude/agents/code-reviewer.md -->
---
name: code-reviewer
description: コード変更のレビュー。差分を読んで問題点を報告する。
tools:
  - Read
  - Grep
  - Glob
model: sonnet
---

あなたはコードレビュアーです。
与えられた差分を読み、以下の観点でレビューしてください:
- バグの可能性
- 設計原則への違反
- テストの漏れ

出力形式:
- 問題の重要度（高/中/低）
- ファイルパスと該当箇所
- 具体的な修正提案
```

ポイント:
- `tools` で使えるツールを制限する（レビュアーに書き込み権限は不要）
- `model` でコストを制御する（レビューならsonnetで十分なことが多い）
- サブエージェントはネストできない（1階層のみ）
- サブエージェントの結果だけがメインに返るので、コンテキストが節約される

### いつ委譲するか

| 状況                                   | やること                                     |
| -------------------------------------- | -------------------------------------------- |
| 「このプロジェクトの構造を把握したい」 | Exploreに委譲                                |
| 「この変更の影響範囲を調べたい」       | サブエージェントに委譲                       |
| 「テスト結果を分析してほしい」         | サブエージェントに委譲                       |
| 「このファイルを修正して」             | メインで直接やる                             |
| 「設計方針を相談したい」               | メインで直接やる（その後セッション切り替え） |

---

## 6. Skills — 段階的開示の中核

セクション0で述べた「段階的開示」を実現する最も有用な仕組みがSkillsである。
CLAUDE.mdは毎回読み込まれるが、Skillsは必要なときだけ読み込まれる。
「常に必要な情報 → CLAUDE.md」「特定タスクで必要な情報 → Skill」と使い分けることで、コンテキストを目的に応じて動的に制御できる。

### skillの作り方

`.claude/skills/` の下にディレクトリを作り、`SKILL.md` を置く:

```markdown
<!-- .claude/skills/deploy/SKILL.md -->
---
name: deploy
description: 本番環境へのデプロイ手順
---

# デプロイ手順

1. mainブランチが最新であることを確認
2. scripts/pre-deploy-check.sh を実行して全チェック通過を確認
3. scripts/deploy.sh production を実行
4. scripts/post-deploy-verify.sh でヘルスチェック

各スクリプトの詳細はスクリプト内のコメントを参照。
```

### skillの設計方針

- skillの中にスクリプトやテンプレートを同梱できる（`.claude/skills/deploy/scripts/` など）
- Claudeにボイラープレートを「生成」させず、同梱ファイルを「使わせる」ことで再現性を上げる
- skillの作り方がわからなければ、Claudeに聞く: `「デプロイ用のskillを作りたい。SKILL.mdのテンプレートを見せて」`

---

## 7. 設定ファイルのベストプラクティス

### .claude/settings.json の構成例

```json
{
  "permissions": {
    "allow": [
      "Read",
      "Grep",
      "Glob",
      "pytest tests/**",
      "ruff check src/",
      "mypy src/ --strict"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "ruff check src/"
          }
        ]
      }
    ]
  }
}
```

設定の方針:
- `permissions.allow` で頻出コマンドの毎回承認を省略する
- hooksでリンター・フォーマッターを自動実行する
- CLAUDE.mdの「お願い」よりhooksの「強制」を優先する（確定的 > 確率的）
- `.claude/settings.json` はgitにcommitしてチームで共有する

### /init で始める

新しいプロジェクトで最初にやること:

```bash
cd your-project
claude
# セッション内で
/init
```

`/init` はプロジェクト構造を分析してCLAUDE.mdの雛形を生成する。ここから不要な記述を削って洗練させていく。

---

## 8. カスタムコマンド — 繰り返し作業のパッケージ化

`.claude/commands/` にMarkdownファイルを置くと、スラッシュコマンドとして使える。

```markdown
<!-- .claude/commands/review-pr.md -->
---
description: PRの差分をレビューする
model: sonnet
---

git diffの結果をレビューしてください。
対象ブランチ: $ARGUMENTS

観点:
- バグの可能性
- テストの漏れ
- CLAUDE.mdのルールへの違反

問題がなければ「LGTM」とだけ返してください。
```

使い方: `/review-pr feature/user-auth`

`$ARGUMENTS` にコマンドの引数が入る。チームで共有するコマンドはプロジェクトの `.claude/commands/` に、個人用は `~/.claude/commands/` に置く。

---

## 9. よく使うコマンド一覧

| コマンド       | 用途                               |
| -------------- | ---------------------------------- |
| `/init`        | CLAUDE.mdの雛形を生成              |
| `/compact`     | コンテキストを要約・圧縮           |
| `/clear`       | コンテキストをリセット             |
| `/model haiku` | 軽量モデルに切り替え               |
| `/model opus`  | 高性能モデルに切り替え             |
| `/effort low`  | 推論の深さを下げる                 |
| `/plan`        | 計画モード（実装前に計画を立てる） |
| `/status`      | 現在の設定状態を確認               |
| `/doctor`      | 環境の問題を診断                   |
| `/resume`      | 過去のセッションを再開             |
| `/hooks`       | 設定済みhookの一覧                 |
| `/agents`      | サブエージェントの管理             |

ワンライナーでの利用:

```bash
# パイプで渡す
cat error.log | claude -p "このエラーの原因を特定して"

# 非対話で実行
claude -p "tests/ 以下のテストを全部実行して結果を報告して"
```

---

## ハンズオン演習

### 演習1: CLAUDE.mdを書く

自分の担当プロジェクト（なければサンプルプロジェクト）に対してCLAUDE.mdを書く。

手順:
1. `claude` を起動し `/init` を実行
2. 生成されたCLAUDE.mdを確認する
3. 以下の観点で手動で編集する:
   - 不要な記述を削る（「これを消したらClaudeが間違えるか？」テスト）
   - ビルド・テスト・リントのコマンドが正確か確認
   - ツールで強制すべきルールをhooksに移す
4. 新しいセッションを立ち上げて、CLAUDE.mdの指示通りにClaudeが動くか確認する

成功基準: Claudeに「このプロジェクトでテストを実行して」と指示したとき、CLAUDE.mdの記載通りのコマンドが使われること。

### 演習2: skillを作る

「よくある作業手順」をskill化する。

手順:
1. Claudeに相談する: `「○○の作業手順をskill化したい。SKILL.mdのテンプレートを作って」`
2. 生成されたテンプレートを `.claude/skills/your-skill/SKILL.md` に配置する
3. skill内で使うスクリプトがあれば同じディレクトリに同梱する
4. 新しいセッションで、そのskillが自動的にトリガーされるか確認する
5. `description` を調整して、意図したタイミングで発動するようにする

成功基準: 関連するタスクを依頼したとき、Claudeが自動的にそのskillを読み込んで使うこと。

### 演習3: サブエージェントに委譲する

同じタスクを「メインで全部やる場合」と「サブエージェントに委譲する場合」で比較する。

手順:
1. まずメインセッションで直接実行: `「このプロジェクトのディレクトリ構造を調べて、主要なモジュール間の依存関係を説明して」`
2. `/clear` でリセット
3. 次に委譲で実行: `「Exploreサブエージェントにこのプロジェクトのディレクトリ構造と主要モジュールの依存関係を調査させて、結果だけ報告して」`
4. 両方のあとに `/compact` の前後でコンテキスト量を確認する（`/status` で確認可能）

成功基準: 委譲した場合のほうがメインコンテキストの消費が少ないことを体感する。

### 演習4: スクリプト化と冪等性

「LLMに直接やらせていた作業」をスクリプトに切り出す。

手順:
1. Claudeに指示: `「tests/ 以下のテストファイル一覧と各ファイルのテスト関数の数を集計するPythonスクリプトを scripts/count_tests.py に作成して」`
2. スクリプトの中身を確認する（Claudeが書いたコードも人間がレビューする）
3. スクリプトを直接実行して結果を確認: `python scripts/count_tests.py`
4. 同じスクリプトを再実行して、同じ結果が出ることを確認（冪等性）
5. 今後同じ集計が必要なときは、Claudeに `「scripts/count_tests.py を実行して」` とだけ指示する

成功基準: 同じ処理を何度実行しても同じ結果が返ること。Claudeに「計算して」ではなく「スクリプトを実行して」と指示する癖がつくこと。

---

## まとめ — 明日から使う3つの習慣

1. **セッションを使い捨てろ。** 相談したら結論をファイルに書き出して、新しいセッションで実装に入る。
2. **ツールに任せろ。** 品質管理はhooksで自動化、計算はスクリプトで。LLMは「使う側」。
3. **コンテキストを守れ。** 探索や分析はサブエージェントに委譲し、メインには結果だけ返させる。タスク固有の知識はSkillsで必要なときだけ読み込む。

これらを意識するだけで、Claude Codeの効果は大きく変わる。
慣れてきたら、カスタムコマンド、agent teams、git worktreeなど、さらに高度な機能に進んでいこう。